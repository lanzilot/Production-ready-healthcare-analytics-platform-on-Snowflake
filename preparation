CREATE OR REPLACE DATABASE hospital_db;
CREATE OR REPLACE SCHEMA hospital_db.bronze;  
CREATE OR REPLACE SCHEMA hospital_db.silver;  
CREATE OR REPLACE SCHEMA hospital_db.gold;    a

SHOW SCHEMAS IN DATABASE hospital_db;

CREATE OR REPLACE FILE FORMAT hospital_db.public.csv_format
    TYPE = 'CSV'
    FIELD_OPTIONALLY_ENCLOSED_BY = '"'
    SKIP_HEADER = 0          -- Change from 1 to 0
    NULL_IF = ('NULL', 'null', '');

CREATE OR REPLACE STAGE hospital_db.public.my_stage
COMMENT = 'Stage for uploading hospital CSV files';
